export enum MessageTypes {
  UserToUser = 1,
  UserToCompany = 2,
  CompanyToCompany = 3,
  ContactUs = 4,
}

export enum AdvertisementTypes {
  Advertisement = 1,
  Inquiry = 2,
}

export enum DemandTypeEnum {
  Invite = 1,
  Request = 2,
  IsMember = 0,
}
